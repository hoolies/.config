# .config

Dotfiles I share as this folder.

Neovim is **not** in here on purpose. `nvim/` is gitignored. That config is LazyVim, plugin-heavy, and local to machines that have it. I do not want to publish or sync it with these files.

Vim is everywhere Neovim is not. `.vimrc` is the portable, plugin-free editor config. It aims at the same habits (leader, clipboard, colors, navigation) without plugins.

## Configurations

* Alacritty
* clipse
* conky
* espanso
* functions
* fuzzel
* glow
* helix
* shell (zsh)
* tmux
* Vim (`.vimrc`)
* xfce4
* yazi
* Backgrounds
